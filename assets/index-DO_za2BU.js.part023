th a modeled ${Math.round(k.towingPenaltyPct*100)}% range hit.${de}`);let pe=w.overflowSelected?` Leftover bed load only — ${Math.round(w.movedCargoLbs).toLocaleString()} lb of kit/bed cargo moved to the trailer.`:C>0?` (includes ${Math.round(C)} lb estimated trade kit)`:``;L&&!w.overflowSelected?fe.push(`Payload at ${Math.round(T).toLocaleString()} lb is over the ${Math.round(F).toLocaleString()} lb rating.${pe} A tow-a-trailer option can move that cargo off the bed.`):L?fe.push(`Leftover bed payload at ${Math.round(T).toLocaleString()} lb is still over the ${Math.round(F).toLocaleString()} lb rating.${pe}`):T>0&&T>=F*.85?fe.push(`Payload is near capacity — keep an eye on people, leftover cargo, and tongue weight together.${pe}`):T>0&&fe.push(`Payload of ${Math.round(T).toLocaleString()} lb is within the ${Math.round(F).toLocaleString()} lb rating.${pe}`),k.equipmentKwh>8&&fe.push(`Powered equipment is a large share of the day's energy — confirm those hours before trusting the reserve.`),k.cargoPenaltyPct>0&&fe.push(`Cargo/kit mass is modeled as a ${Math.round(k.cargoPenaltyPct*100)}% range hit${C>0?` (~${Math.round(C)} lb estimated trade kit)`:``}.`);let me;me=s?.isUnknown?`Charging is unknown, so this fit is conservative: about 70% start and no assumed overnight or daytime charger. Confirm home, shop, or a daytime top-up before trusting this number.`:!B&&!ce?`This day needs a charger you do not have yet. Overnight Level 2 at home or the shop is the usual fix.`:!B&&ce?`Plan on a daytime top-up. Without overnight charging the truck may not start the day full.`:B&&N<25?`Overnight charging covers a normal start, but the tight reserve means a mid-day top-up is worth having as backup.`:ce?`Overnight charging plus a daytime top-up is more than this day needs — a comfortable setup.`:`Home or shop overnight charging is enough — start the day full, the same way you would with a full tank.`;let he=[];R&&he.push(w.overflowSelected?`The trailer (empty curb weight plus moved kit/bed cargo) is over this truck's towing rating.`:`The trailer is over this truck's towing rating.`),L&&he.push(w.overflowSelected?`Leftover bed payload is still over this truck's payload rating after moving kit/bed cargo to the trailer.`:`The payload is over this truck's payload rating. A tow-a-trailer option can move kit/bed cargo off the bed.`),z&&he.push(`Estimated driving + equipment energy exceeds the usable battery with the charging you selected.`),!B&&!ce&&!s?.isUnknown&&he.push(`There is no reliable way to recharge before the next day.`);let ge=le===`not-a-fit`?he:le===`conditional`?[N<20?`End-of-day reserve is about ${Math.round(N)}%, under the 20% target.`:null,B?null:s?.isUnknown?`Charging is unknown, so overnight charging is not assumed.`:`No overnight charging is selected.`].filter(Boolean):[],_e=[...he,`A much longer route or a lot more powered equipment than you entered`,E===0?`Adding a heavy trailer you did not model — or skipping a tow-a-trailer option when the bed is over capacity`:`Towing more than the empty-plus-cargo trailer weight you modeled, especially at highway speed`,B?`Losing overnight charging for a stretch of days`:`Still having no reliable overnight charge`,`Cold weather, hills, or cabin HVAC — none of those are in this estimate`],ve=k.kwhPerMile>0?k.equipmentKwh/k.kwhPerMile:0,ye=Math.max(0,Number(n)||0)*.8,be=!L&&T>0&&T>=F*.85,xe=!R&&E>0&&E>=I*.85,Se=!z&&N<20,Ce=Si(c),we=v!=null&&(Number(e)||0)<(Number(v)||0)-.5,Te=Ji({effectiveRange:ie,dailyMiles:Math.max(0,Number(t)||0)+ye+ve,trailerWeight:E,towingCapacity:i,payloadWeight:T,payloadCapacity:o,homeCharging:B,categoryScore:l,jobFit:p??x(d,f),kwhPer100Miles:y},{payloadOver:L,towOver:R,energyShort:z,payloadNear:be,towNear:xe,rangeTight:Se,bedWeightLbs:Ce.bedWeightLbs,kitWeightLbs:Ce.kitWeightLbs,configRangeHit:we,hasOvernight:B}),Ee=Te.score,De=[];return(h||g||_)&&De.push([h,g,_].filter(Boolean).join(` / `)),{rating:le,ratingLabel:ue,summary:Pi({vehicleName:m,categoryLabel:u,rating:le