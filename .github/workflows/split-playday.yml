name: Split Play Day preview JS parts (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled; Map My Day preview uses assemble-mapmyday.yml"
